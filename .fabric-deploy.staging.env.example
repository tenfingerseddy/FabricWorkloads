# Fabric Deployment Configuration — Staging
# Copy to .fabric-deploy.staging.env and fill in values

# Required: Azure AD / Entra credentials
FABRIC_TENANT_ID=
FABRIC_CLIENT_ID=
FABRIC_CLIENT_SECRET=

# Workspace configuration
FABRIC_WORKSPACE_NAME=ObservabilityWorkbench-Staging
FABRIC_KQL_DATABASE=EH_Observability

# Eventhouse endpoints (required — no hardcoded defaults)
EVENTHOUSE_QUERY_ENDPOINT=
EVENTHOUSE_INGESTION_ENDPOINT=
# Legacy alias (still supported, but prefer the above)
# FABRIC_KQL_ENDPOINT=

# Notebook schedule configuration (cron expressions)
SCHEDULE_INGESTION=*/15 * * * *
SCHEDULE_CORRELATION=*/30 * * * *
SCHEDULE_ALERTS=0 * * * *
